using Ams.Infrastructure.Attribute;
using ${options.ModelsNamespace}.${options.SubNamespace};

namespace ${options.RepositoriesNamespace}
{
    /// <summary>
    /// ${genTable.FunctionName}
    /// 仓储实体
    /// @Author: ${GeneratorReplaceDto.Author}(Davis.Cheng)
    /// @Date: ${GeneratorReplaceDto.CreateTime}
    /// </summary>
    [AppService(ServiceLifetime = LifeTime.Transient)]
    public class ${GeneratorReplaceDto.ModelTypeName}Repository : BaseRepository<${GeneratorReplaceDto.ModelTypeName}>
    {
    }
}